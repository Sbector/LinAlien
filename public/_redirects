/* ============================================================
   Netlify SPA redirect — serves index.html for all routes
   ============================================================ */
/*    /index.html   200
